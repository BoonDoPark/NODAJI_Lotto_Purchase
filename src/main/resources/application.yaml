server:
  port: 8000
spring:
  application:
    name: lotto-payment
  datasource:
    url: jdbc:postgresql://localhost:5432/user_lotto_info?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
